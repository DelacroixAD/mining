{
  "name": "sustainability-simulator",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "dev": "npm run dev --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client"
  }
}
